<%- include('partials/header') %>

<div class="bg-white rounded-lg shadow-md p-6">
    <h1 class="text-2xl font-bold mb-6">Bricothèque Association Platform Gains</h1>
    
    <div class="mb-8">
        <h2 class="text-xl font-semibold mb-4">Monthly Statistics</h2>
        <div class="overflow-x-auto">
            <table class="min-w-full">
                <thead class="bg-gray-50">
                    <tr>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Month</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Total Rentals</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Commission Earned</th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200">
                    <% stats.forEach(function(stat) { %>
                        <tr>
                            <td class="px-6 py-4 whitespace-nowrap"><%= stat.month %></td>
                            <td class="px-6 py-4 whitespace-nowrap"><%= stat.total_rentals %></td>
                            <td class="px-6 py-4 whitespace-nowrap">€<%= stat.total_commission.toFixed(2) %></td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
    </div>

    <div class="bg-blue-50 p-6 rounded-lg">
        <h2 class="text-lg font-semibold mb-3">About Our Commission Structure</h2>
        <p class="text-gray-700">
            As a non-profit organization, we apply a fair commission structure to maintain the platform:
        </p>
        <ul class="list-disc ml-6 mt-2 text-gray-700">
            <li>€2 for tools with daily rate under €30</li>
            <li>€5 for tools with daily rate between €30-€50</li>
            <li>€10 for tools with daily rate over €50</li>
        </ul>
        <p class="mt-4 text-gray-700">
            All proceeds go towards platform maintenance and community projects.
        </p>
    </div>
</div>

<%- include('partials/footer') %>